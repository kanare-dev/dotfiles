# Starship プロンプト設定
# 詳細: https://starship.rs/config/

# 先頭の空行を出さない
add_newline = false

# プロンプトのフォーマット
format = """
[](fg:#585858)\
$os\
$username\
$hostname\
$battery\
[](fg:#585858 bg:#0087AF)\
$directory\
[](fg:#0087AF)\
$fill\
[](fg:#444444)\
$git_branch\
$git_status\
$git_commit\
$nodejs\
$python\
$rust\
$golang\
$aws\
$docker_context\
$package\
$cmd_duration\
$localip\
$time\
[](fg:#444444)
[╰─────](bold cyan)$character"""

# 左右分割（右寄せ用）
[fill]
symbol = " "

# os
[os]
style = "bg:#585858 fg:#F2F2F2"
format = "[ $symbol ]($style)"
disabled = false
[os.symbols]
Macos = "" #リンゴのicon
Ubuntu = "" #Ubuntuのicon

# プロンプト記号
[character]
success_symbol = "[>](bold green) "
error_symbol = "[>](bold red) "
vimcmd_symbol = "[>](bold green) "

# バッテリー（バッテリー駆動時に表示されやすくする）
[battery]
full_symbol = "󰁹"
empty_symbol = ""
charging_symbol = "󰂄"
discharging_symbol = "󰂀"
unknown_symbol = "󰂑"
format = "[ $symbol$percentage ]($style)"

[[battery.display]]
threshold = 30
style = "bg:#585858 fg:#CCCC11"

# ユーザー名（通常は非表示、SSH接続時のみ表示）
[username]
show_always = true
format = "[  $user ]($style)"
style_user = "bg:#585858 fg:#F2F2F2"
style_root = "bg:#585858 fg:#FF5F5F"

# ホスト名（通常は非表示、SSH接続時のみ表示）
[hostname]
ssh_only = true
format = "[  $hostname ]($style)"
style = "bg:#585858 fg:#CFCFCF"

# ディレクトリ
[directory]
style = "fg:#303030 bg:#0087AF"
read_only = ""
read_only_style = "fg:#AF0000 bg:#0087AF"
format = "[ 󰉋 $path ]($style)[$read_only]($read_only_style)"
truncation_length = 4
truncation_symbol = "…/"
truncate_to_repo = true
home_symbol = "󰋜"
use_os_path_sep = false
fish_style_pwd_dir_length = 2

[directory.substitutions]
"Documents" = ""
"Downloads" = ""
"Music" = ""
"Pictures" = ""

# Git ブランチ
[git_branch]
symbol = ""
style = "bg:#444444 fg:#CF9562"
format = "[ $symbol $branch(:$remote_branch) ]($style)"
truncation_length = 20
truncation_symbol = "…"

# Git ステータス（変更がある場合のみ表示）
[git_status]
style = "bg:#444444 fg:#CFCFCF"
format = "[$all_status$ahead_behind]($style)"
conflicted = " 󰞇${count}"
up_to_date = ""
untracked = " 󰋗 ${count}"
stashed = " 󰏗${count}"
modified = " 󰏫${count}"
staged = " 󰄬${count}"
renamed = " 󰁕${count}"
deleted = " 󰆴${count}"
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"

# Git コミット（detached HEAD時など）
[git_commit]
style = "bg:#444444 fg:#CFCFCF"
format = "[  $hash$tag ]($style)"
commit_hash_length = 4
only_detached = true
tag_symbol = " 🔖 "

# Git メトリクス（無効化）
[git_metrics]
disabled = true

# Node.js
[nodejs]
format = "[  $version ]($style)"
symbol = ""
style = "bg:#444444 fg:#7CFC00"
detect_files = ["package.json"]
detect_folders = ["node_modules"]

# Python
[python]
format = "[  $version (${virtualenv}) ]($style)"
symbol = ""
style = "bg:#444444 fg:#FFD75F"
detect_files = [".python-version", "Pipfile", "__pycache__", "pyproject.toml", "requirements.txt", "setup.py", "tox.ini"]
detect_folders = []

# Rust
[rust]
format = "[  $version ]($style)"
symbol = ""
style = "bg:#444444 fg:#FF5F5F"
detect_files = ["Cargo.toml", "Cargo.lock"]
detect_folders = []

# Go
[golang]
format = "[  $version ]($style)"
symbol = ""
style = "bg:#444444 fg:#5FD7FF"
detect_files = ["go.mod", "go.sum", "glide.yaml", "Gopkg.yml", "Gopkg.lock", ".go-version"]
detect_folders = ["Godeps"]

# Docker コンテキスト
[docker_context]
format = "[ $symbol$context ]($style)"
symbol = "🐳 "
style = "bg:#444444 fg:#5F87FF"
only_with_files = true
detect_files = ["docker-compose.yml", "docker-compose.yaml", "Dockerfile", ".dockerignore"]
detect_folders = []

# パッケージ情報（package.json など）
[package]
format = "[ $symbol$version ]($style)"
symbol = "📦 "
style = "bg:#444444 fg:#FFAF5F"
display_private = false

# 実行時間（1秒以上かかったコマンドのみ表示）
[cmd_duration]
min_time = 1_000
format = "[  $duration ]($style)"
style = "bg:#444444 fg:#CCCC11"

# 時刻（右側に表示）
[time]
disabled = false
format = "[  $time ]($style)"
style = "bg:#444444 fg:#F2F2F2"
use_12hr = false
time_format = "%T"

# SSH時のローカルIP表示（必要なときだけ）
[localip]
ssh_only = true
format = "[ 󰩟 $localipv4 ]($style)"
style = "bg:#444444 fg:#CFCFCF"
disabled = false

# AWS（AWS_PROFILE があるときだけ profile を表示）
[aws]
disabled = false
format = "[ 󰸏 $profile ]($style)"
style = "bg:#444444 fg:#FFAF5F"
